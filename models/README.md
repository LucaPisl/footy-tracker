This directory is deprecated.

Place all model weights under `weights/` instead:
- `weights/yolo/` for the player detector
- `weights/jersey/` for the jersey number model

The repo ignores files under `models/` to avoid accidental commits.
